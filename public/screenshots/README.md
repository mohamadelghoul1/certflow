Real screenshots for the public site.

Drop these here and the homepage shows them in place of the drawn
mockups, in the same browser frame:

- dashboard.png — the dashboard, about 1600 x 1000, on a demo project
  with an invented address (never a real client's).
- portal.png — the client portal's project page, same size.

Nothing else in this folder is read.
